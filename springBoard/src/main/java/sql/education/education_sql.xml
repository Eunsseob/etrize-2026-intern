<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="education">
    
    <resultMap type="com.spring.education.vo.EducationVo" id="educationVo">
    	<result property="name" 	column="NAME"/>
    	<result property="phone" 	column="PHONE"/>
    </resultMap>
    
     <insert id="insertEducationList" parameterType="java.util.List">
        INSERT ALL
        <foreach collection="list" item="edu">
            INTO EDUCATION (EDU_SEQ, SEQ, SCHOOL_NAME, DIVISION, START_PERIOD, END_PERIOD, MAJOR, GRADE, LOCATION)
             VALUES (
            EDU_SEQ.NEXTVAL,
            #{edu.seq, jdbcType=VARCHAR},
            #{edu.schoolName, jdbcType=VARCHAR},
            #{edu.division, jdbcType=VARCHAR},
            #{edu.startPeriod, jdbcType=VARCHAR},
            #{edu.endPeriod, jdbcType=VARCHAR},
            #{edu.major, jdbcType=VARCHAR},
            #{edu.grade, jdbcType=VARCHAR},
            #{edu.location, jdbcType=VARCHAR}
        	)
        </foreach>
        SELECT * FROM DUAL
    </insert>
</mapper>
