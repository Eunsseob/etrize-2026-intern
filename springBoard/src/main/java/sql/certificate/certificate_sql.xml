<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="certificate">
    <resultMap type="com.spring.certificate.vo.CertificateVo" id="certificateVo">
	    <result property="certSeq" column="CERT_SEQ"/>
	    <result property="seq" column="SEQ"/>
	    <result property="qualifiName" column="QUALIFI_NAME"/>
	    <result property="acquDate" column="ACQU_DATE"/>
	    <result property="organizeName" column="ORGANIZE_NAME"/>
	 </resultMap>
    
    <!-- 자격증을 넣기위한 쿼리문 -->
     <insert id="insertCertificateList" parameterType="com.spring.certificate.vo.CertificateVo">
        INSERT INTO CERTIFICATE (CERT_SEQ, SEQ, QUALIFI_NAME, ACQU_DATE, ORGANIZE_NAME)
            VALUES (
            CERT_SEQ.NEXTVAL,
            #{seq, jdbcType=VARCHAR},
            #{qualifiName, jdbcType=VARCHAR},
            #{acquDate, jdbcType=VARCHAR},
            #{organizeName, jdbcType=VARCHAR}
        	)
	  </insert>
	  
	  <!-- 자격증을 삭제하기 위한 쿼리문 -->
	  <delete id="deleteCertificate" parameterType="String">
		    DELETE FROM CERTIFICATE WHERE SEQ = #{seq}
	</delete>
	
	<!-- 자격증을 가져오기 위한 쿼리문 -->	
	<select id="getCertificateList" parameterType="String" resultMap="certificateVo">
	    SELECT * FROM CERTIFICATE WHERE SEQ = #{seq}
	</select>
	
	<!-- CER_SEQ 있으면 Update -->
	<update id="updateCertificateList" parameterType="com.spring.certificate.vo.CertificateVo">
	    UPDATE CERTIFICATE 
	    SET QUALIFI_NAME = #{qualifiName}, 
	        ACQU_DATE = #{acquDate}, 
	        ORGANIZE_NAME = #{organizeName}
	    WHERE CERT_SEQ = #{certSeq}
	</update>
	
	<!-- CER_SEQ 없으면 Insert -->
	<!-- DB에는 있지만, 리스트에 없다면? Delete -->
	<delete id="deleteCertificateByCertSeq" parameterType="com.spring.certificate.vo.CertificateVo">
	    DELETE FROM CERTIFICATE WHERE CERT_SEQ = #{certSeq}
	</delete>
</mapper>
